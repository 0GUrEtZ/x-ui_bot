# 3X-UI Telegram Bot

Telegram-бот для управления пользователями 3X-UI панели с поддержкой регистрации, продления подписок и мониторинга трафика.

## 🚀 Возможности

- **Регистрация пользователей** через бота с одобрением администратором
- **Автоматическое продление** подписок с сохранением UUID/паролей
- **Поддержка всех протоколов**: VMESS, VLESS, Trojan, Shadowsocks
- **Мониторинг трафика** и статистики клиентов в реальном времени
- **Безлимитные подписки** с защитой от случайного изменения
- **Умное округление** дней подписки (учитывает часы)
- **Управление клиентами**: блокировка, разблокировка, просмотр статистики

## 📦 Быстрый старт

### Docker (рекомендуется)

1. Склонируйте репозиторий:
```bash
git clone https://github.com/0GUrEtZ/x-ui_bot.git
cd x-ui_bot
```

2. Создайте конфигурацию:
```bash
cp config.yaml.example config.yaml
nano config.yaml  # Отредактируйте настройки
```

3. Запустите:
```bash
docker-compose up -d
```

### Локально

1. Установите Go 1.25+
2. Установите зависимости:
```bash
go mod download
```

3. Создайте `config.yaml` и запустите:
```bash
go run ./cmd/bot
```

## ⚙️ Конфигурация

```yaml
telegram:
  token: "YOUR_BOT_TOKEN"      # Токен от @BotFather
  admin_ids:
    - 123456789                 # Ваш Telegram ID (получите через /id)
  proxy: ""                     # Опционально: SOCKS5 прокси
  api_server: ""                # Опционально: кастомный API сервер

panel:
  url: "https://panel.example.com"  # URL 3X-UI панели
  username: "admin"                  # Логин администратора
  password: "password"               # Пароль администратора
```

## 🎯 Команды

### Для пользователей:
- `/start` - Главное меню
- `/id` - Получить свой Telegram ID
- `📱 Получить VPN` - Получить конфигурацию подписки
- `📊 Статус подписки` - Детальная информация о подписке
- `🔄 Продлить подписку` - Запросить продление у администратора

### Для администраторов:
- `/clients` - Список всех клиентов с трафиком
- `/status` - Статус сервера
- `/usage <email>` - Статистика конкретного клиента
- Одобрение/отклонение регистраций и продлений

## 🔧 Особенности

- **Округление дней**: 89 дней 24 часа = 90 дней (округление вверх)
- **Безопасное продление**: Сохраняет UUID, пароли и subId при продлении
- **Защита безлимитных**: Нельзя случайно изменить безлимитную подписку
- **Валидация email**: Защита от пустых значений и ошибок ввода
- **Адаптивная клавиатура**: Кнопки меняются в зависимости от типа подписки

## 📝 Логи

Просмотр логов:
```bash
docker logs x-ui-bot --tail=50 -f
```

## 🛠 Разработка

Пересборка с очисткой кэша:
```bash
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

## 📄 Лицензия

MIT License

## 🤝 Вклад

Pull requests приветствуются! Для крупных изменений сначала откройте issue.

---

**Примечание**: Бот работает только с 3X-UI панелью. Требуется доступ к API панели.
