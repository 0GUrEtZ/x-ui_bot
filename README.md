# 3X-UI Telegram Bot

Telegram-бот для автоматизации управления VPN-подписками через 3X-UI панель.

## 🚀 Возможности

- **Регистрация клиентов** через бота с одобрением администратором
- **Система оплаты** с настраиваемыми тарифами (1 месяц, 3 месяца, 6 месяцев, 1 год)
- **Автоматическое продление** подписок с сохранением UUID/паролей
- **Поддержка всех протоколов**: VMESS, VLESS, Trojan, Shadowsocks
- **Управление клиентами**: блокировка/разблокировка, удаление, просмотр статистики
- **Отправка инструкций** по подключению (ссылка на облако с гайдами)

## 📦 Быстрый старт

### Docker (рекомендуется)

1. Склонируйте репозиторий:
```bash
git clone https://github.com/0GUrEtZ/x-ui_bot.git
cd x-ui_bot
```

2. Создайте конфигурацию:
```bash
cp config.yaml.example config.yaml
nano config.yaml  # Отредактируйте настройки
```

3. Запустите:
```bash
docker-compose up -d
```

### Локально

1. Установите Go 1.25+
2. Установите зависимости:
```bash
go mod download
```

3. Создайте `config.yaml` и запустите:
```bash
go run ./cmd/bot
```

## ⚙️ Конфигурация

```yaml
telegram:
  token: "YOUR_BOT_TOKEN"      # Токен от @BotFather
  admin_ids:
    - 123456789                 # Ваш Telegram ID (получите через /id)
  proxy: ""                     # Опционально: SOCKS5 прокси
  api_server: ""                # Опционально: кастомный API сервер

panel:
  url: "https://panel.example.com"  # URL 3X-UI панели
  username: "admin"                  # Логин администратора
  password: "password"               # Пароль администратора

payment:
  bank: "Сбербанк"                   # Название банка для оплаты
  phone_number: "+79001234567"       # Номер телефона для переводов
  instructions_url: ""               # Ссылка на облако с инструкциями (Яндекс.Диск, Google Drive)
  prices:
    one_month: 300                   # Цена за 30 дней (₽)
    three_month: 800                 # Цена за 90 дней (₽)
    six_month: 1500                  # Цена за 180 дней (₽)
    one_year: 2800                   # Цена за 365 дней (₽)
```

## 🎯 Команды

### Для пользователей:
- `/start` - Главное меню
- `/id` - Получить свой Telegram ID
- `📱 Получить VPN` - Получить конфигурацию подписки
- `📊 Статус подписки` - Детальная информация о подписке
- `🔄 Продлить подписку` - Запросить продление (с выбором тарифа и оплатой)

### Для администраторов:
- `/clients` - Список всех клиентов с управлением (блокировка, удаление)
- `/status` - Статус сервера
- `/usage <email>` - Статистика конкретного клиента
- Одобрение/отклонение регистраций и продлений с отображением @username

## 📋 Логи

Просмотр логов:
```bash
docker logs x-ui-bot --tail=50 -f
```

## 🛠 Разработка

Пересборка с очисткой кэша:
```bash
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

## 📌 Особенности

- Автоматическое сохранение UUID и паролей при продлении
- Защита от случайного удаления конфигураций
- Отображение Telegram username (@username) в запросах администратору
- Интеграция с облачным хранилищем для инструкций по подключению
- Система тарифов с автоматическим расчётом срока действия

---

**Требования**: 3X-UI панель с доступом к API
