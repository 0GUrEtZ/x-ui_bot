services:
  x-ui-bot:
    build: .
    container_name: x-ui-bot
    restart: unless-stopped
    volumes:
      - ./config.yaml:/root/config.yaml:ro
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
